# Keep the generated catalog byte-identical on checkout.
#
# `sbsave-tools` writes JSON with platform newlines (CRLF on Windows) and the
# integration test compares the regenerated bytes against the committed file,
# so the worktree must always contain CRLF regardless of `core.autocrlf`.
data/catalog.json text eol=crlf